# OpenSpec Configuration — pipeline tesis
# Auto-generated by sdd-init on 2026-06-05

context: >
  Piano sample library pipeline for Kontakt 7 (engineering thesis).
  Python 3.13 stack: librosa, numpy, scipy, soundfile, matplotlib.
  3-pipeline architecture: piano_sampler.py → pipeline_compare.py → pipeline_rectify.py.
  Non-destructive processing chain for multi-session piano recordings.
  Source separation via MVSep external model.
  Instrument: historical (un-tunable) piano — flutter treated as controlled variable.

strict_tdd: false

testing:
  runner: null
  runner_available: false
  layers:
    unit: null
    integration: null
    e2e: null
  coverage: null
  quality_tools:
    linter: null
    type_checker: null
    formatter: null
  note: >
    No test runner, linter, or formatter installed. SDD Strict TDD mode disabled.
    Recommended: install pytest for unit tests before introducing test-driven changes.

phases:
  sdd-propose:
    required: true
  sdd-spec:
    required: true
  sdd-design:
    required: true
  sdd-tasks:
    required: true
  sdd-apply:
    required: true
  sdd-verify:
    required: false
    note: Verify skipped until test infrastructure is available

persistence:
  mode: hybrid
  openspec_path: openspec/
  engram_enabled: true

delivery:
  strategy: force-chained
  review_budget: 400
  pace: automatic
